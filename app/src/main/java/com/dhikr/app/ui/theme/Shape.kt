package com.dhikr.app.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.unit.dp

val PillShape = RoundedCornerShape(999.dp)
val CardShape = RoundedCornerShape(28.dp)
val DialogShape = RoundedCornerShape(30.dp)

// 22dp per design/README.md's Radius token table: list rows and stat tiles get
// 22dp, distinct from the 26-30dp used by cards, dialogs and panels (CardShape).
val ListRowShape = RoundedCornerShape(22.dp)
